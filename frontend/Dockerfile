# Cicero web UI — built once, then served as static files by nginx (A6).
#
# nginx also reverse-proxies the API paths to the backend service, so the SPA
# talks to a same-origin API: no CORS, and EventSource (the live debate stream)
# works without any cross-origin configuration.
FROM node:20-alpine AS build

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:1.27-alpine AS runtime

COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
